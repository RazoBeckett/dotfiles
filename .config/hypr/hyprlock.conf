general {
  hide_cursor = true
}

background {
  monitor =
  path = $HOME/Pictures/Wallpapers/My Wallpapers/butterfly-neon-glowing-dark-background-amoled-3840x2160-2171.png
  blur_passes = 0
  color = rgba(26, 27, 38, 1.0)
}

label {
  monitor =
  text = $TIME
  color = rgba(E10052EE)
  font_size = 90
  font_family = SF Pro Display
  position = -4, -60
  halign = center
  valign = top
}

label {
  monitor =
  text = cmd[update:43200000] date +"%A, %d %B %Y"
  color = rgba(E10052EE)
  font_size = 25
  font_family = SF Pro Display
  position = -4, -200
  halign = center
  valign = top
}

input-field {
  monitor =
  size = 200, 50
  outline_thickness = 4
  dots_size = 0.2
  dots_spacing = 0.2
  dots_center = true
  inner_color = rgba(000001AA)
  font_size = 20
  font_color = rgba(E10052EE)
  font_family = DankMono Nerd Font Italic
  fade_on_empty = false
  placeholder_text = Isn’t she cute?
  hide_input = false
  check_color = rgba(122, 162, 247, 1.0)
  fail_color = rgba(247, 118, 142, 1.0)
  fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
  capslock_color = rgba(224, 175, 104, 1.0)
  position = -10, -300
  halign = center
  valign = center
}
